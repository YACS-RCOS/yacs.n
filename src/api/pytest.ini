# pytest.ini
[pytest]
minversion = 6.2.5
addopts = -ra
markers =
    testclient: tests updated to use the FastAPI TestClient object
    incompletedependency: marks tests that depend on an api that has not yet been completed
    tortoise: tests that are converted to tortoise
testpaths =
    tests/
