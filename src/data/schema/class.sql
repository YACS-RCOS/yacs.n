-- -- class as in the abstract class of CSCI-1200 i.e. data structure, that has sections, that each have class_session
-- drop table class;
-- create table class(
--   department varchar(255),
--   level      int,
--   title      varchar(255),
-- )
-- consider having class maybe
