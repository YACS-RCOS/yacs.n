version: '3.4'

# web (front-end) depends on api (back-end) depends on (db)
# web -> api -> db [dependency chain]

services:
  yacs_web:
    ports:
      - 80:80
    environment:
      # https://docs.docker.com/compose/compose-file/#variable-substitution
      - HOST=${HOST:-localhost}
      - ADMIN_PASS=${ADMIN_PASS:-difficult_password}

  yacs_api:
    restart: always
    environment:
      - ENV=production
      - DEBUG=False
      - DB_HOST=yacs_db
      - DB_USER=yacs
      - DB_PASS=${DB_PASS:-easy_dev_pass}
      - DB_NAME=yacs
      - WORKERS=${API_WORKERS:-4}
      - FLASK_SIGN_KEY=${FLASK_SIGN_KEY:-secretKey}

  yacs_db:
    environment:
      - POSTGRES_DB=yacs
      - POSTGRES_USER=yacs
      - POSTGRES_PASSWORD=${DB_PASS:-easy_dev_pass}
